# 開発記録

マイルストーンごとに、作ったものと実機で分かったこと。仕様は [設計](設計.md)、部品ごとの実測は [spikes/README.md](../spikes/README.md)。
日付は作業した日。

**M1 ブリングアップ (Tab5 + QR)**
- `tools/serial.ps1` を作り、書き込み済みの工場ファームの出力を**安全に読めるか**を最初に確かめる (無音ならリスク 3 の手順で戻す)
- 最小スケッチを焼く: 起動ログ・チップ rev (→ `ChipVariant` 確定)・PSRAM 容量・画面に日本語・縦向きの `setRotation`・タッチ座標をシリアルへ・`screenshot`
- I2C スキャン (Port A): **0x21 あり / 0x29 なし**を確認。QR 読取ループ →  `tools/test_qr.html` の QR をかざしてもらう
- `tests/run.ps1` で MSVC のホストテストが回ることを確認

**M2 省電力・音・保存の土台**
- C6 の電源 OFF / USB-A 5V OFF / マイク・IMU OFF。INA226 の値と (あれば) USB 電力チェッカーで前後を比べる
- CPU 360 ⇄ 40 ⇄ 20MHz をシリアルコマンドで切り替え、**I2C (QR)・DSI・USB シリアル**がそれぞれ動くか。パネルスリープとタッチ
- **消灯中の手動ライトスリープ**: 200ms ごとのタイマ復帰で QR (I2C) の読取が回るか / 復帰後に DSI (画面の再点灯)・PSRAM・I2C・SPI・USB シリアルが正常か /
  QR をかざしてから反応するまでの遅れ / 電流がどれだけ下がるか → **問題なければ採用**。
  USB シリアルはスリープ中に切れるので、結果は起床後にまとめてシリアルへ出す (または NVS に残して後で `dump`)
- スピーカー: 3 段階の鳴らし方を耳で、明るさ 5〜100% を目で確かめる
- `partitions.csv` の反映と arduino-cli での指定方法 / `tooldb` の読み書き / 記号入り 15 文字のキー / `nvs_entry_find` / `nvs_flash_erase_partition` / USB 抜きで保持
- RTC の設定と保持

**M3 カメラ・SD (go/no-go)**
- `ESP_Video` で SC202CS の検出 → M5Unified との内部 I2C 共有 → 1 フレーム → 270° 回転 → HW JPEG → `drawJpgFile` で表示 → `screenshot` で確認
- 取り込み停止/センサ電源 OFF からの撮影までの時間と電流
- SD (microSD があれば): マウント / 抜き差しの検出 / 長いファイル名 / 100 枚超の削除 / 書込中の電源断
- **ここで Arduino のままいけないと分かったら、先へ進まず方針を見直す**

**M4 core を TDD (ホスト MSVC、M1〜M3 と並行)**
- `code_classifier` → `config` → `time_format` → `drawer_detector` → `power_manager` → `photo_store` → `csv_export` → `loan_book` → `session`

**M5 統合ファーム (ToF はシリアル注入)**
- `power_hw` → `qr_reader` (別の機器 (M5Stack Basic) で使っていた間欠トリガ・READY/読み戻し検証/再初期化を移植) → `drawer_source` (sim) → `nvs_kv_store` → `alarm` → `rtc_clock` → `camera` → `sd_card` → `serial_port`
- 画面: 一覧 → セッション → 無登録の写真 → 写真ビューア → 履歴・ログ → 名札だけの一覧 → 検索 → 設定画面 12 項目 → 記録領域を読めない画面
- **シリアル越しに通し試験** (`sim open` / `sim qr ...` / `dump` / `screenshot`) + 実物の QR・画面・音は人が確認
- **1 段目 骨組み (2026-09-13)**: `ToolCheck.ino` / `partitions.csv` (`spikes/m2_nvs` のものをそのまま) / `hw/` `nvs_kv_store` `rtc_clock` `power_hw` `serial_port` / `app/app` / `ui/jp_text`。
  設定値の保存と名前での読み書き (`core/config_store`)、`time set` の現地時刻の読み取り (`core/time_format`) はホストで TDD。
  core を riscv gcc で初めてビルドし、`--warnings all` でプロジェクト側の警告は 0 (出た 10 件はライブラリ)。フラッシュは 1,858,032 バイト (28%)
  - spike の残り (`loans` に 32 バイトの blob 400 件・`app` 3 件) が入ったまま起動し、**「読めなかった貸出が 400 件あります (消さずに残しています)」と報告して消さない**ことを確認 (NVS の使用量が変わらない)。
    `nvs erase records` / `nvs erase all` / `nvs erase all yes` (小文字) / `nvs erase everything YES` は
    どれも `#ERR` で拒否し、NVS の使用量 (1213 エントリ) と読めなかった貸出 400 件が変わらないことを確認 (確認のうえ実行)
  - **NVS データ削除 2 種 (2026-09-13 決定で spike の残りを消した)**: `nvs erase records YES` → 読めなかった貸出 0 件・設定 (明るさ 60) は残る・開閉ログの 1 件目が `records_cleared`。
    続けて `nvs erase all YES` → 再起動して `cfg=not_found`・NVS の使用 0 エントリ・名前空間 0・`dump all` は全部 0 件。
    記録の削除は `clearNamespace` が読み書きで開くので、中身が空の名前空間が残る (使用 24 エントリ・名前空間 6)
  - `cfg set brightness_pct 80` → `cfg get` → 再起動で残る (`cfg=loaded`) / 範囲外 (5%)・無い名前・読めない値は理由付きで拒否 / `time set 2026-09-13T20:24:07` (現地時刻) で RTC に入る、書式違いは拒否
  - USB シリアル: `info` `report` `time` `time set` `cfg` `cfg get|set <名前> <値>` (名前は Config のメンバ名) `dump cfg|loans|hist|openlog|alerts|all` `nvs erase records|all YES` `screenshot` `restart`。
    `dump` の各セクションの 1 行目は見出し (rows= には数えない)。記録領域を読めないときに受け付けるのは `info` `report` `time` `screenshot` `restart` `nvs erase all YES` だけ
  - RTC: M5Unified の `getVoltLow()` はフラグレジスタ 0x1D の VBLF (bit7) しか見ない。発振停止の VLF (bit1) も読み、どちらかが立っていれば時刻未設定として扱い、`time set` で両方落とす。
    実際に立つかは未確認 (今の値は 0x25 = VBFF・RSF・UF)
  - 焼いた直後に COM3 を開いても再起動しなかった (`rst:0x17` は電源投入後の初回だけ)。起動時の `#BOOT` は、開く前に出したものも読めた
  - ~~スクリーンショットの PNG では白が水色、緑が青に写る~~ **誤り (2026-09-13 4 段目で訂正)**: 画面そのものがその色だった。
    文字の色を `uint32_t` の引数で渡していたので、LovyanGFX が RGB565 の定数 (`TFT_WHITE` = 0xFFFF) を RGB888 として読んでいた
    (塗りつぶしは RGB565 のまま渡っていて正しかった)。`ui/jp_text` の色の引数を `uint16_t` に直した。確かめずにスクリーンショットのせいにしていた
- **2 段目 QR・セッション・警告音 (2026-09-13)**: `hw/qr_reader` (M5UnitQRCode を使わない自前ドライバ。`requestFrom` の失敗を検出・READY は 0 以外なら読み出す・長さが異常でも読み出す・
  トリガモードは読み戻して確かめる・読めない状態が 20 回続いたら作り直し、3 回作り直しても読めなければ「読めません」・間欠 1400 / 700ms (6 段目で配線)・自動スキャンにした直後 500ms の読み取りは捨てる) /
  `hw/alarm` (M2 で聞き分けた鳴らし方) / `app` にセッションの配線、`session`、`sim open|close|qr <コード>|remove <行>|switch <行>|pause <分>|resume` (開発フラグ `dev_sim` が ON のときだけ)。
  画面は 4 段目までの仮で、下半分に案内・経過秒・行・直前の出来事を出す。フラッシュ 1,925,104 バイト (29%)、プロジェクト側の警告 0
  - シリアル注入で通した: 開発フラグ OFF の `sim` を拒否 / 開放 → 物品 2 件 → 名札で確定 → 20 秒後に開閉ログ / 不正な QR (空白入り・1 文字) を読み捨て /
    セッションの外の名札で貸出中の一覧 (記録しない) / 返却・代理返却・持出に切替 (元の貸出を返却漏れの印で閉じて付け替え) /
    警告 1 → 警告 2 → 打ち切りで利用者不明の持出 6 件と赤帯 / 何も読まない開放の打ち切りで無登録開放の赤帯 / 一時停止の開始・停止中の開放 (開閉ログだけ)・停止中の QR の登録 (音なし)・再開
  - 確認: 警告音は「開けた直後は無音 → 10 秒で警告 1 (断続) → 15 秒で警告 2 (交互) → QR を読むと止まる」のとおり (試験用に警告 1 = 10 秒・警告 2 = 15 秒・打ち切り 30 秒にした) /
    実物の物品 QR (`test11111`) と名札 `1234567` で、操作音 2 回・画面の案内・記録まで通った (名札までに 15 秒を過ぎて警告 2 まで鳴り、名札で止まった)。
    試験中に自分で QR (`AA12345` など) もかざした。QR の読み取り 13 回で I2C のエラー 0、自動にした直後の読み捨て 0
- **3 段目 撮影・microSD (2026-09-13)**: **撮影も SD も別のタスクにした**。SD の確認は抜けている間 1 回約 3 秒止まり、撮影は開放から約 0.6 秒かかるので、画面・タッチ・QR・警告を止めないため。
  `hw/camera_task` (起動時に初期化して取り込みを止めておく。頼まれたら内部 I2C を押さえて取り込み、`capture_delay_ms` を過ぎた最初のフレームを使い、押さえを離してから PPA で回転・ハードウェア JPEG。直近の 1 枚を PSRAM に持つ) /
  `hw/sd_task` (マウント・5 秒ごとの抜けの確認・保存・古い写真の削除。SD に触るのはこのタスクだけ) /
  `hw/internal_i2c` (内部 I2C の押さえ。メインループは押さえられたときだけ `M5.update()` と RTC を読む。取り込みが 5 秒を超えたら押さえを待たずに読んで `#ERR`。`captureBuffer` の `VIDIOC_DQBUF` はタイムアウトしないため) /
  `app`: 開放で写真名を決めて撮影を頼む (記録と SD で同じ名前、同じ秒は `-2`)。SD が無いときは保存を頼まない。アイドル (セッション無し・引き出し閉・10 秒無操作) で古い写真を消す。
  `cam` `photo test|show` `sd` `sd prune|check`。`camera_enabled` の入切は再起動で反映。起動時に `M5.Speaker.begin()` を済ませる (最初に鳴らすときの begin が IO エキスパンダ = 内部 I2C に触るため)。
  タスクからはシリアルに出さず、結果をキューで返してメインループが出す。フラッシュ 2,535,714 バイト (38%)、プロジェクト側の警告 0
  - 実機: カメラの初期化 340ms / テスト撮影で最初のフレーム 42〜53ms・使ったフレーム 508〜546ms (9〜15 枚目。フレームの間隔は約 33ms で、M3 のループの中の約 65ms より短い)・回転と JPEG 58〜59ms・45〜92KB /
    `sim open` → 撮影 → SD に保存 42ms → 名札で確定した開閉ログに同じ写真名 (`20260913-220249.jpg`) → 10 秒の無操作の後に古い写真の削除 (100 枚以下なので 0 枚)
  - 確認: Tab5 の前に立って撮った写真は、上下の向きが正しく顔が入った (立った姿勢だけ。しゃがむ・横からは未確認。`capture_delay_ms` は 500 のまま)
  - **不具合を見つけて直した**: SD の抜けを `SD.exists("/ToolCheck")` だけで確かめていたら、カードを約 10 秒抜いても `present=1` のままだった
    (M3 で `exists` が失敗したのは、毎秒の書き込みと交互に呼んでいたからとみる)。FatFs が読んだセクタを覚えていてカードに問い合わせないとみて、
    セクタ 0 を直接読む `SD.readRAW` に変えた → 抜いて 600ms で「SD なし」、挿し直して 281ms でマウントし直した。抜いている間もシリアルの応答 (約 2 秒ごと) は途切れず、画面の時計も進んだ
  - **(解決 2026-09-14)** スクリーンショット (PNG 約 12 万バイト) の base64 が 1 回だけ壊れて保存できなかった (取り直しは送った大きさと一致)。原因は USB シリアルの行落ちと同じ (6 段目の「USB シリアルの行落ち」)
- **4 段目 画面 (2026-09-13〜14)**: 画面の操作はこう決めた: **長い一覧はページ送りのボタン / 名札だけの一覧は 10 秒かタップで閉じる / 放置したタブは 2 分で貸出中の 1 ページ目に戻る / 検索もこの段で作る**。
  どの画面を出すかは `core/screen_state` (ホストで TDD)、描くのとタッチは `app/ui_controller`、寸法と色は `ui/theme`。変わった区画だけ描き直す
  - 4a: ヘッダ (時刻・一時停止の残り・QR / SD / カメラの ●) / 赤帯 / 貸出中 (経過の長い順・前日は赤)・返却履歴 (代理・返却者不明・返却漏れ・取消)・開閉ログの一覧とページ送り / タブ /
    セッションの全面表示 (案内・警告までの残りとバー・警告 1 は赤の点滅・警告 2 は赤・行と「持出に切替 / 返却に戻す」「×」) / 名札だけの一覧 / 記録領域を読めない画面
  - 4b: 赤帯に写真 (1/3 に縮めて PSRAM の画像に作り置く。帯のタップで全面) / 一覧の写真のある行のタップで全面 / 写真の全面表示 (直近に撮った 1 枚は RAM、それ以外は SD タスクで読む。描くのに 342ms)
  - 4d: 検索 (上に入力欄、中に候補 5 件、下に英数キーボード。赤帯は出さない)。候補は `LoanBook::searchItems`
  - 試験用に `sim touch <x> <y>` (タッチの注入) と `sim mute on|off` (消音、保存しない) を足した (どちらも開発フラグ ON のときだけ)
  - **実機で見つけて直した**: 文字の色を `uint32_t` で渡していて RGB888 として読まれていた (1 段目の「スクリーンショットの色ずれ」は誤りだった) /
    QR で行が増えても画面に知らせず行が描かれなかった / 案内の行が 1 行目に重なった / 「✕」(U+2715) が日本語フォントに無く四角になった → 「×」/
    開閉ログの 2 行目が右端の「写真」に重なった → 0 件の数を省き、ページ送りの手前で切る (`setClipRect`)
  - **USB シリアルの行落ちを確かめた**: 「保存しました」と出たスクリーンショットが開けず、PNG の大きさが装置の知らせた大きさより 93 バイト少なかった (行が丸ごと落ちると base64 としては読めてしまう)。
    写真入りの大きな PNG では 2 回に 1 回ほど起きる。`tools/serial.ps1` は大きさが合わなければ保存しないようにした。
    `dump` も `rows=` と行数を突き合わせる。原因は 6 段目で分かって直した (6 段目の「USB シリアルの行落ち」)
  - **2026-09-14 に実機で触って**: 起動音と操作音がうるさい → 操作音の既定を 0% (無音) に (`config`、TDD)、起動時に `M5.Speaker.begin()` を呼ばない
    (鳴らすときに内部 I2C を押さえてから始める)。起動音が `Speaker.begin()` の音だったかは耳では未確認。音量は 5 段目の設定画面の「音量」(試し鳴らし付き) で上げる
- **5 段目 設定画面 (2026-09-14)**: タブを 4 つ × 150px にして右端に「設定」(**1 秒の長押し**で開く。触っただけでは開かず案内を出す) → PIN → 12 項目の一覧 (今の値の要約付き)。
  PIN の照合とロック (`core/pin_lock`) と設定画面の出し方 (`core/screen_state` に `Settings`) はホストで TDD。描くのとタッチは `app/settings_ui`、記録や設定を変える操作は `SettingsActions` として `App` に頼む。
  保存を押すまで反映しない (明るさだけはその場で試し、戻ると元に戻す)。セッションが始まったら閉じ、未保存の変更は捨てる。無操作 2 分で貸出中に戻る
  - 5b-1: PIN (5 回失敗で 1 分ロック) / 数値の編集 (明るさ / 音量と試し鳴らし / 警告の秒数 / 写真とテスト撮影 / 利用者 ID の桁数。制約違反は理由を出して保存しない) / 警告の一時停止 (10 / 30 / 60 分・今すぐ再開) / 時刻 (年月日時分の ▲▼ と時差)
  - 5b-2: 赤帯・貸出の取消 (赤帯は「確認して消す」、貸出は 2 回押すと取消で返却履歴に「取消」で残る) / USB シリアル出力 (貸出中・返却履歴・開閉ログ・赤帯・設定値・すべて) /
    NVS データ削除 (件数を見せ、「記録の削除」「全削除」は 3 秒押し続けると実行。進みを帯で出す) / 機器情報 / PIN の変更 (2 回入力) /
    「記録領域を読めません」画面にも PIN 無しの全削除 (3 秒押し続ける)。ToF 校正は M6、動作ログの入切は 6 段目 (→ 2026-09-14 に取りやめ。決定事項)
  - **2026-09-14: 警告音も 0% (無音) にできるようにする** (持ち出しの記録だけを取りたい現場もあるため)。`config` の範囲を 0〜100% に (TDD)。音量 0 ではスピーカーに触らず、警告は画面の赤だけで知らせる
  - 試験用に `sim hold <x> <y>` (長押し) と `sim press <x> <y> <ms>` / `sim release` (押し続け) を足した。夜中の試験は消音のまま行い、削除は 3 秒に届かない押し方で帯だけを確かめた (記録は消していない)
  - **実機で見つけて直した**: 音量の「試す」ボタンが「- / +」に 2px 重なった / 離しているループのたびに「離した」を知らせていたので `sim press` の押し続けがすぐ消えた (離した瞬間だけ知らせる形に) / 「全削除」の説明文がボタンからはみ出した
- **6 段目 省電力の配線 (2026-09-14)**: `core/power_manager` を `App` に持たせた。QR の読み取り (読めたものだけ)・引き出しの開放・タッチを操作として知らせ、
  セッション・設定・写真・名札の一覧・検索・撮影の間は操作中。変わったものだけ反映する: 明るさ (減光・消灯・警告中は最大) / QR の間欠 (減光・消灯) / SD の確認間隔 (減光・消灯は 30 秒)。
  減光・消灯中の最初のタッチは起こすだけ (その押し方の長押し・押し続けにも使わない)。`#POWER` で状態の変化、`power` コマンドと機器情報に状態
  - 実機 (消音): 減光までを 1 分にして、減光 (明るさ 15%・QR 間欠・SD の確認 30 秒) が続くこと、最初のタッチは起こすだけで 2 回目は操作になることを確認
  - **実機で見つけて直した**: 減光中の間欠読み取りで、トリガを開けた直後に、かざしていないのに読めない中身 (長さ 10、NUL や記号混じり) が返り、それで毎回起きて減光が続かなかった
    (M2 の手動 ⇄ 自動の切替直後の `"aA` と同じ種類)。減光・消灯中の読めない QR では起こさず、記録だけ出して捨てる
  - **スピーカーは入切しない (2026-09-15 がこのままと決定)**: 当初の表では待機以下で止めるとしていたが、状態が変わるたびに止めたり始めたりすると始めるときの音が出るおそれがある (2026-09-14「起動音がうるさい」)。M2 の実測で差は約 0.01A
  - **状態ごとの電流 (2026-09-16 実測、USB 電力チェッカー、カメラは使わない設定)**: 操作中 0.49A・4.91V (明るさ 60%) / 減光 0.35A・4.95V (明るさ 5%) / 画面消灯 0.26A・4.96V (明るさ 0%)。
    待機 (Idle) は測っていない。M2 の spike の実測 (基準 0.57A → 一番絞って 0.25A) と同じ範囲
  - **不具合を見つけて直した**: ループの先頭で取った時刻を、コマンドの処理の後に `Session::alarmLevel` へ渡していたので、`sim open` の直後に警告 2 が一瞬鳴った
    (セッションを進めた時点より「今」が前になり、経過時間が uint32 で一周した)。コマンドの処理より後に時刻を取る形にした (`Session` は `now_ms` が減らない前提)
  - **USB シリアルの行落ち (2 段目で 1 行欠けた・4 段目でスクリーンショットが壊れた) の原因を突き止めて直した (2026-09-14)**:
    `Serial` は HWCDC (USB Serial/JTAG)。書くたびに `HWCDC::isPlugged()` (SOF をティックで見張る) を見て、false なら「未接続」の道に回し、
    送信リング (既定 256 バイト) が一杯なら古いバイトを捨てて新しいバイトを入れる。`isPlugged()` は PC が読んでいる最中でも一瞬 false にゆらぐ。
    - 確かめ方: 写真の全面表示 (PNG 約 45 万バイト) のスクリーンショットで、行ごとに「書く前にリングが足りない」「`isPlugged()` が false」を数えた。
      10 回中 7 回欠け、欠けた回は必ず false の行があり (44〜93 行)、欠けなかった回は 0。リングはほぼ全行で一杯だった (行の途中からも欠ける)
    - 直し方: 書く前に `Serial.availableForWrite()` が行の長さ以上になるまで待つ (`SerialPort::waitWritable`。未接続の道でも空きがあれば捨てない)。
      スクリーンショットと `dump` の各行に入れた。送信リングも `Serial.setTxBufferSize(4096)` で広げた (普段のログが一杯に当たりにくいように)
    - 結果: 同じ条件で 10 回中 10 回とも大きさが合った (false の行があった回も合った。1 回 約 7 秒)。`dump all` の `rows=` も全セクションで合った。
      `#END png` に `unplugged=` (false と見えた行の数) を出す
    - PC が本当に抜けているとリングは減らないので、待ちは 500ms で諦め、その後 2 秒は待たずに書く (設定画面の「USB シリアル出力」を PC なしで押しても行ごとに止まらないように)
  - **PC に繋いだままポートを閉じていると、書き込みでメインループが止まっていた → 直した (2026-09-14)**:
    Windows はポートを閉じると USB シリアルを読まないが、SOF は来るので HWCDC は「繋がっている」と見て、一杯のリングへの書き込みを 1 回につき最大 2 秒
    (待ち 100ms × 20 回) 待つ。PC の USB から給電したまま使うと、ログが 4096 バイトたまった後は行を出すたびに画面とセッションが止まる。
    - 確かめ方: `info` にループの間隔の最大 `loop_max_ms` と 200ms 以上の回数 `loop_slow` を足した (出したら戻す)。ポートを開いて `dump all` を 5 回送り、読まずに閉じて 20 秒待った
    - 直す前: `loop_max_ms=20460` (閉じている間ずっと止まった)。ポートを開いて読んでいるときは `loop_max_ms=6`
    - 直し方: `Serial.setTxTimeoutMs(0)` (空きの無い行は待たずにまるごと捨てる)。まとめて出すときは上の `waitWritable` で空きを待つので、読んでいれば落ちない。
      待ちが 0 だと `Serial.flush()` は送り終える前にリングを捨てるので、再起動の前 (`restart` / `nvs erase all`) は `SerialPort::drain` (リングが空くまで最大 300ms) に替えた
    - 直した後: 同じ試験で `loop_max_ms=507` (1 行ぶんの待ち 500ms で諦めた 1 回だけ)。ポートを開いた状態でスクリーンショット 3 回・`dump all` の `rows=`・`#OK restart` の返事とも問題なし
    - PC を繋がずに (電源だけで) 使うときは SOF が来ないので「未接続」の道で捨てるだけで、待たない作り (実機では未確認)
- **7 段目 引き出しセンサを使わない設定・SD の写真の全削除 (2026-09-14、Tab5 を繋がずに作った。実機では未確認)**:
  - core (ホストで TDD): `config` に `drawer_sensor_enabled` (保存は [15] の bit4 に「使わない」で持つ) / `session` は使わない設定の名札の確定で撮影を求める / `open_log` に `PhotosCleared`
  - `hw/drawer_sensor` (0x29 の応答を 5 秒ごと) / `hw/sd_task` に写真の全削除 / `app`: 名札の確定で撮るときだけ写真名の同じ秒の数を進める、使わない設定では `sim open|close` を `#ERR`、
    設定の入切をその場で反映、`sd erase photos YES`、`#BOOT` と `info` に `drawer_sensor=` `tof=` / 設定画面: 「引き出しセンサ」(入切と説明)、「データ削除」の 3 つ目のボタン /
    ヘッダ: 使う設定のときだけ ToF● (見つからなければ赤)。左の見出しと時刻は右の ● の手前で切る
  - フラッシュ 2,599,426 バイト (39%)。`--clean --warnings all` でプロジェクト側の警告 0 (出た 54 件はコア 48・M5GFX 6)。
    **`--clean` を付けないと前のオブジェクトを使い回して警告が 1 件も出ない** (全体で 0 件と出た)
  - 全削除の途中でカードが抜けたら、続けて 3 回消せなかった時点で打ち切る (抜けている間は 1 回に数秒かかるため。M3)
  - **実機 (2026-09-14 夜。消音・カメラ OFF)**:
    - 書き込みの前に COM3 を開くと `rst:0x17` で再起動した (4 回目の観測)。その直前の起動は `qr=0`、再起動後は `qr=1` (電源投入直後は QR ユニットの立ち上がりが間に合わないとみる。
      `QrReader` は見つからなければ 5 秒ごとに探し直すので、PC を繋がなくても後から読める作り)。書き込みのリセットの後に開いたときは再起動しなかった
    - 使う設定で ToF 無し: `#BOOT drawer_sensor=1 tof=0` と起動の案内 / ヘッダの ToF● が赤 / 25 秒待っても 0x29 の問い合わせのエラーログは出ない
    - **ヘッダの時刻が ToF● で「9/14(月) 2」と切れた → 収まらないときは見出しを省いて時刻を左に寄せた。直した後は「9/14(月) 23:32」が全部見える**
    - 設定の「引き出しセンサ」(切り替えると説明の明るさが入れ替わる。保存はしていない) / 一覧の要約「使う (見つかりません)」 /
      「データ削除」の 3 つ目のボタン (`sim press` 1.5 秒で帯が半分。消していない) に見切れなし
    - `cfg set drawer_sensor_enabled 0` → `sim open|close` は `#ERR` / `sim qr NOTOF-01` → `sim qr 1234567` で持出、20 秒後に開閉ログ `opened=0`・写真名なし
      (カメラ OFF なので撮影は頼まない) / ヘッダに ToF● が出ない / `sd erase photos` (YES なし・小文字の yes) は `#ERR`
  - **実機 続き (2026-09-14 夜。カメラを一時的に入れ、SD の写真を消した)**:
    - 使わない設定で `sim qr PHOTO-01` → `sim qr 1234567`: 名札で `#CAPTURE name=20260914-234139.jpg` → 撮影 OK (使ったフレーム 709ms・72KB) → SD に保存 (10 → 11 枚) →
      20 秒後の開閉ログと、貸出 `PHOTO-01` の持出の写真名が同じ
    - 設定画面の「データ削除」の 3 つ目を 3 秒押し続け (`sim press 360 1045 3000`): `#SD photos_deleted removed=11 photos=0 ms=124` /
      画面に「SD の写真を 11 枚消しました」、ボタンの説明は「写真 0 枚」 / 開閉ログの先頭に `photos_cleared` (一覧の表示は「SD の写真を全部削除」)
    - 試験の後にカメラを切り直した (`camera_enabled` 0)。引き出しセンサは使わないのまま。試験の貸出 `NOTOF-01` `PHOTO-01` が残っている
- **8 段目 一番長くなる表示の見切れと消灯の確認 (2026-09-15、実機・消音・カメラ OFF)**:
  - 消灯までの移り変わり: `off_after_min` を 2 分に縮めて、減光 (明るさ 15%・QR 間欠) → 消灯 (明るさ 0・QR 間欠) / 消灯中に名札の QR で起きて名札だけの一覧 /
    消灯中の最初のタッチは起こすだけ (`#POWER wake`)。`off_after_min` は 5 に戻した (`dim_after_min` 1 は 6 段目の試験のまま)
  - 本体の時計を 5 日戻して 15 文字の物品 (`ABCDEFGHIJKLMNO` `WWWWWWWWWWWWWWW`) を持出にし、すぐ PC の時刻に戻した (1 秒以内)
  - **見つけて直した** (`ui::drawTextFit`: 入らなければ Large → Medium → Small と縮め、それでも入らなければ切る):
    - 貸出中・名札だけの一覧: W 15 文字が「120時間前」に重なった → 物品番号を経過時間の手前まで縮める
    - 返却履歴: 物品番号の後ろの印 (代理など) が日時に重なって画面の外へ出た (`ABCDEFGHIJKLMNO` でも) → 物品番号は日時の手前まで縮め、印は 2 行目の右へ
    - セッションの行: 返却の行で番号が「持出に切替」のボタンの下に隠れた (`ABCDEFGHIJKLMNO` でも) → 1 行目は物品番号だけ、種類と「借 1234567」は 2 行目へ。
      切替ボタンを 200 → 170px
    - 一覧の下の知らせが画面の右から出た → 右端で切る。確定の知らせは 0 件の数を省く。読めない QR (空白・カンマ) の文言を「空白・カンマなどの文字は使えません」に短くした
    - 検索と設定画面の貸出の取消の一覧も同じ描き方にした (検索は直す前から見切れなし。取消の一覧は実機で撮っていない)
  - 直した後に撮り直して重なりなし: 貸出中 (W 15 文字は 1 段縮む) / 返却履歴 (W 15 文字は Small まで縮み、印は 2 行目の右) /
    セッションの返却・持出に切替・持出・記録済みの行 (W 15 文字は Small まで縮んでボタンの手前) / 読めない QR の知らせ (全部見える)
  - 撮り直していないもの: 確定の知らせの新しい文言 (記録の後 5 秒しか出ず、撮るのが間に合わなかった) / 「120時間前」と W 15 文字の組み合わせ (付け替えで古い貸出が閉じた)
  - 検索: 「A」「W」で候補を出して見切れなし

**M6 ToF 到着後**
- **1 段目 PaHub と Unit ToF4M を使えるようにする・開閉の判定と校正 (2026-09-15)**:
  - 段 A: `core/i2c_route` (TDD) と `hw/port_a_bus`。Wire を `QrReader` から `PortABus` に移し、PaHub (0x70〜0x77) と QR・ToF の ch を探して繋ぐ。
    5 秒ごとに PaHub の読み戻しとユニットの応答を見て、壊れた・見つからないなら探し直す。`#I2C` / `i2c` / `i2c rescan`、機器情報に Port A
    - 実機: `#I2C hub=0x70 qr=ch0 tof=ch1 search_ms=4`、`qr=1 qr_fw=0x03`。DIP「全部 HIGH」で 0x70
  - **届いた ToF は VL53L1X の Unit ToF4M だった** (0x010F = 0xEACC。Pololu VL53L0X の `init()` が 0ms で失敗して気付いた)。Pololu VL53L1X 1.3.1 を入れ、`hw/drawer_sensor` を作り替えた
    (距離モード Medium・測定 33ms・`dataReady()` を見てから `read(false)`。I2C の失敗 3 回か結果が来ないと見失ったとみて 1 秒ごとに探し直す。初期化に失敗したら型番をログに出す)
  - 段 B: `core/tof_reading` (TDD: 使える距離・読み取りの状態・校正の中央値)。距離を `DrawerDetector` に渡して `#DRAWER open|close src=tof`。測定間隔は電力状態 (50 / 100 / 200ms)、基準を取る間は操作中。
    `tof` / `tof raw <秒>` / `tof calibrate` (2 秒の中央値を保存)。設定画面「引き出しセンサ」に校正 (設計の「設定画面」の表)。ヘッダの ToF● は 緑 / 黄 = 未校正 / 赤 = 見つからない・使えない値が続く
  - 実機 (音量 0・カメラ OFF・机の上で約 1.6m 先を見ている): 初期化 41ms・1580 ± 3mm・状態 0、ループの最大 10ms /
    `tof calibrate` → 1579mm (減光中の 200ms 間隔で 11 個しか集まらず下限すれすれ → 基準を取る間は操作中にした) / 画面の「基準を取る」→ 1582mm (43 個、下書きだけで保存しない)・戻ると判定を再開 /
    基準を 1800mm にして `#DRAWER open src=tof` → セッション → 名札 (`sim qr`) で確定の記録 (赤帯なし) / 基準を戻すと `#DRAWER close src=reset` / 設定画面と機器情報 (14 行) に見切れなし
  - **実機確認 (2026-09-15): どれも問題なし** — QR・ToF の ch の差し替え / PaHub を外して直結 / PaHub の抜き差しで繋ぎ直す / QR を 1 枚かざして読める / 手をかざして開 → 離して約 1 秒で閉 /
    ToF を抜くと ToF● 赤・挿し直すと測り直す。画面の明るさ 10%・減光時 5% に変えても問題なし
- (当初の計画) `drawer_source` を ToF に切替。取り付け → 各段の距離を `tof raw` で CSV 化 → 閾値決定 → ToF 校正画面 → 実開閉での通し試験 (リスク 6 の判断)
- **2 段目 返却の自動化・持出の取りやめ・利用者 ID の英数字化・ヘッダの ● (2026-09-15)**
  - `core/session`・`core/loan_book`・`core/open_log` (TDD) を作り替え。設計の「セッション」節の内容 (返却の自動化・持出に切替・取りやめ) を実装
  - `core/status_marks` (新規、TDD): ヘッダの ● を出すかと色を判定。`core/code_classifier`・`core/config`・`core/config_store` に `user_id_alnum` (保存は [15] の予備 bit5)
  - 実機確認 (`tools/serial.ps1`・音量 0):
    - 貸出中の物品を読む → その場で返却 (名札不要・返した人は記録しない)・警告なし・20 秒で `#RECORD end=confirmed returns=1 user=` (空)
    - 返却済みの行「持出に切替」→ 名札待ちの切替・もう一度押すと返却に戻る (本は書き直さない) / 名札で確定 → 貸出に `switched=1`、返却履歴は通常の返却 (返却漏れにならない)
    - QR だけの持出を「取りやめ」→ 何も残らない (`#CANCEL cancelled=1 records=0`) / 開けてから持出を読んで取りやめ → 開閉ログに `opened=1 cancelled=1`・**赤帯は増えない**
    - `cfg set user_id_alnum 1` → `TANAKA_` (7 文字) が名札として通る (`kind=user`) / `0` に戻すと物品番号扱いに戻る
    - ヘッダ: カメラ OFF・SD 保存 OFF の状態で **カメラ●・SD● が出ず** ToF●・QR● だけ表示。設定画面「利用者 ID・PIN」に英字・記号の入切と文字数、見切れなし
    - セッション画面: 警告の秒の右に「取りやめ」ボタン、バーは幅 480 に縮めて重ならず。返却の行は「返却済み」ラベル。開閉ログに「取りやめ」の印、返却履歴は「借 %s」だけ (返した人は表示しない)
    - `pwsh -NoProfile -File tests/run.ps1` 400 tests 0 failures / `arduino-cli compile --clean --warnings all` 通過 / 実機クラッシュ・再起動なし (uptime 連続)
  - **追加 (2026-09-16)**: 返却完了ボタン / 設定画面を開いている間は開閉を判定しない / 登録後は閉の範囲に 2 秒続けて戻るまで次の開放を作らない (設計の「セッション」「引き出し検知」「設定画面」)。
    TDD RED 415 tests 32 failures → GREEN 0 failures。版 `0.6.2-m6.3`。`--clean --warnings all` でプロジェクト側の警告 0
  - 実機確認 (2026-09-16 朝。音量 0。**実機は校正済み: 基準 1588mm・開の閾値 210mm**。ToF は 16〜18mm で反応したままだった = 何かがすぐ前にある):
    - 起動直後に `#DRAWER open src=tof` でセッションが始まった (起動は登録ではないので押さえない)
    - 貸出中の `R-TEST-4` を読む → 返却済みの行と「返却完了」ボタン (取りやめと同じ場所、文字にも行にも重ならない) → `sim complete` ですぐ `#RECORD end=confirmed returns=1`・`#DRAWER hold reason=record`
    - 設定画面を開く → `[ToF] 開閉の判定を止めます (設定画面・校正中)`・`sim open` は断る → 閉じる → 判定を再開 (`#DRAWER close src=reset`)。
      **ToF が反応したまま (開の範囲) でも 10 秒以上開放を作らなかった** (前の作りなら閉じた直後に開放と判定していた)
  - **実機確認 (2026-09-16): どちらも問題なし** — ToF の前を空けて、反応が消えてから 2 秒で次の開放が通る / 名札で確定した後に手をかざしたままだと次の開放にならない
- **3 段目 名札先の画面にも「完了」ボタン (2026-09-16)**: 「すぐに次の人も登録したい」。
  `Session::completeTagWindow` (TagWindow のときだけ、20 秒たったときと同じ `SessionEnd::Confirmed` の 1 件で終える) を追加し、
  「続けて物品を読めます」の画面に取りやめ・返却完了と同じ場所で「完了」ボタンを出す。`App::completeTagWindow`、シリアル `sim complete` は
  そのとき終えられる方 (返却だけ / 名札先) を終える。TDD RED 419 tests 14 failures → GREEN 0 failures。版 `0.6.3-m6.4`。
  `--clean --warnings all` でプロジェクト側の警告 0
  - 実機確認 (2026-09-16 夜。`sim mute on`・カメラ OFF):
    - 物品 `X-TAG-01` → 名札 `1234567` → `state=tag_window can_complete_tag=1`、画面に「完了」ボタン
    - `sim complete` → すぐ `#COMPLETE kind=tag records=1`・`#RECORD end=confirmed user=1234567 checkouts=1`・`state=idle`
    - 続けて次の人: `X-TAG-02` → `7654321` で `tag_window` に入り、持出を登録できた
    - **画面の不具合を 1 件見つけて直した**: 案内「名札を読んでから 20 秒は、同じ人で記録します」を「完了」ボタンの左端までの幅に縮めていたため、
      Small でも入らず「同じ人で記」で切れた。ボタンは案内の行より下 (`y + kSessionCancelDy`) から始まり縦に重ならないので、幅の制限を外して全幅に戻した。
      撮り直して全文が見え、ボタンとも重ならない
    - 物品番号が 7 文字だと、英数字の利用者 ID の設定 (実機は `user_id_alnum` 1) では名札として読まれる (`X-TAG-1` が `kind=user`)。仕様どおり
  - **実機確認 (2026-09-16): 「完了」を指で押して押しやすい。問題なし**

- **4 段目 設定画面に「このソフトについて」(2026-09-17)**: 開発者情報とライセンス全文を本体で見られるようにした。入り口は設定一覧の 13 行目 (PIN の後)、リリース日は手で書く定数、GitHub の URL は文字と QR、ライセンスは全文をページ送りで (Arduino コアの LGPL は注記だけ)
  - core (TDD): `about.h` (版数 `0.6.4-m6.5` とリリース日) / `license_text` (埋め込んだ全文が `LICENSE` と `licenses/` の元ファイルと一致するかをテスト) / `text_pager` (UTF-8 の折り返しとページ、80 桁の改行をつなぐ)。RED 439 tests 33 failures → GREEN 0 failures / つなぐ指定 RED 442 tests 1 failures → GREEN 0 failures
  - 実機 (消音): 一覧 13 行が下端 1218 に収まる / このソフトについて (日本語の開発者名・URL・QR) / 節の一覧 / 各節のページに見切れなし。IPA フォントのライセンス 20,444 バイトは 424 行・17 ページで折り返し 32ms
  - **実機で見つけて直した**: 注記の「同梱」の「梱」が日本語フォントに無く四角になった (M5GFX の日本語フォントは IPA フォントの抜粋で、収録は `ja.map` の 4,503 字)。書き換えて、ライセンス本文 5 本にフォントに無い字が無いことを `ja.map` と突き合わせて確かめた / 注記の 2 行目が右端で切れた → 4 行に分けて短く / 英文のライセンスが 80 桁の改行と画面の折り返しでガタガタになった → `text_pager` で決まった桁の改行をつないで流し込む

**M7 試行** (これから) → 開閉ログ・電池の持ちから閾値・状態の時間を調整する
